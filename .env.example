# Copy this file to `.env.local` and fill in the correct connection string.
# The DATABASE_URL should be a standard PostgreSQL URI, for example:
#   postgresql://user:password@host:port/database?sslmode=require

# Connection string for Neon/PostgreSQL
DATABASE_URL=postgresql://neondb_owner:npg_9CdmbMVYvat1@ep-floral-hill-a1y8vxri-pooler.ap-southeast-1.aws.neon.tech/carDB?sslmode=require&channel_binding=require

# Alternative individual parameters (will be used if DATABASE_URL is empty)
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=postgres
DB_PASS=
DB_NAME=postgres
DB_SSLMODE=require